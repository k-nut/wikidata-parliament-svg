{
  "dependencies": {
    "parliament-svg": "^2.0.1",
    "virtual-dom": "^2.1.1"
  },
  "scripts": {
    "start": "esbuild app/app.js --bundle --watch --outfile=dist/bundle.js",
    "build": "esbuild app/app.js --bundle --outfile=dist/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.12.17"
  }
}
